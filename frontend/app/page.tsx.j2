import Link from "next/link";

import { ThemeToggle } from "@/components/theme-toggle";

const stackRows = [
  ["API", "FastAPI"],
  ["Data", "SQLAlchemy"],
  ["UI", "Next.js"],
  ["Infra", "Docker"],
];

const shortcuts = ["Dashboard", "Admin", "Auth"];

const authRows = [
  ["Register", "ready"],
  ["Login", "ready"],
  ["Verify", "local"],
  ["Reset", "local"],
];

export default function HomePage() {
  return (
    <main className="ds-page">
      <div className="ds-atmosphere" />
      <div className="ds-shell">
        <nav className="ds-nav">
          <Link href="/" className="ds-mark">
            {{ project_name }}
          </Link>
          <div className="ds-nav-actions">
            <Link href="/login" className="ds-pill ds-pill-outline">
              Log in
            </Link>
            <ThemeToggle />
          </div>
        </nav>

        <section className="ds-landing">
          <div className="ds-intro">
            <p className="ds-label">DevStack Starter</p>
            <h1 className="ds-display">{{ project_name }}</h1>
            <p className="ds-copy">
              A generated product shell with auth, infrastructure, and protected
              application surfaces already composed.
            </p>
          </div>

          <div className="ds-snap-flow" aria-label="Starter overview">
            <article className="ds-premium-card ds-premium-card-dark ds-snap-card">
              <div className="ds-media-layer ds-media-project" />
              <div className="ds-card-header">
                <span>Generated</span>
                <span>01</span>
              </div>
              <div className="ds-card-content">
                <div>
                  <p className="ds-card-kicker">Project Profile</p>
                  <h2>{{ project_name }}</h2>
                  <p>
                    A full-stack workspace with service ports, environment files,
                    local commands, and backend structure in place.
                  </p>
                </div>
                <div className="ds-works-list">
                  <span>Backend</span>
                  <span>Frontend</span>
                  <span>Services</span>
                </div>
              </div>
            </article>

            <article className="ds-premium-card ds-premium-card-light ds-snap-card ds-center-card">
              <div className="ds-card-header">
                <span>Runtime</span>
                <span>02</span>
              </div>
              <div className="ds-card-content ds-card-content-light">
                <div>
                  <p className="ds-card-kicker">Collection Insights</p>
                  <h2>Stack status</h2>
                </div>
                <div className="ds-chart" aria-hidden="true">
                  <span className="ds-chart-bar-1" />
                  <span className="ds-chart-bar-2" />
                  <span className="ds-chart-bar-3" />
                  <span className="ds-chart-active ds-chart-bar-4" />
                </div>
                <div className="ds-data-list">
                  {stackRows.map(([label, value]) => (
                    <div key={label}>
                      <span>{label}</span>
                      <strong>{value}</strong>
                    </div>
                  ))}
                </div>
                <div className="ds-shortcuts">
                  {shortcuts.map((item) => (
                    <span key={item}>{item}</span>
                  ))}
                </div>
              </div>
            </article>

            <article className="ds-premium-card ds-premium-card-dark ds-snap-card">
              <div className="ds-media-layer ds-media-auth" />
              <div className="ds-card-header">
                <span>Access</span>
                <span>03</span>
              </div>
              <div className="ds-card-content">
                <div>
                  <p className="ds-card-kicker">Auth Detail</p>
                  <h2>User and admin flow</h2>
                  <p>
                    Starter screens cover account creation, sessions, local email
                    verification, password recovery, and admin-only routes.
                  </p>
                </div>
                <div className="ds-data-list ds-data-list-dark">
                  {authRows.map(([label, value]) => (
                    <div key={label}>
                      <span>{label}</span>
                      <strong>{value}</strong>
                    </div>
                  ))}
                </div>
                <div className="ds-card-actions">
                  <Link href="/register" className="ds-pill ds-pill-primary">
                    Create account
                  </Link>
                  <Link href="/dashboard" className="ds-pill ds-pill-outline ds-pill-on-dark">
                    Dashboard
                  </Link>
                </div>
              </div>
            </article>
          </div>
        </section>
      </div>
    </main>
  );
}
