import Link from "next/link";

import { ThemeToggle } from "@/components/theme-toggle";

const stackItems = [
  ["Backend", "FastAPI service"],
  ["Frontend", "Next.js app router"],
  ["Auth", "Email, reset, verify"],
  ["Access", "User and admin roles"],
];

export default function HomePage() {
  return (
    <main className="ds-page">
      <div className="ds-shell">
        <nav className="ds-nav">
          <Link href="/" className="ds-mark">
            {{ project_name }}
          </Link>
          <div className="flex items-center gap-2">
            <Link href="/login" className="ds-pill ds-pill-outline">
              Log in
            </Link>
            <ThemeToggle />
          </div>
        </nav>

        <section className="grid min-h-[calc(100vh-6rem)] items-center gap-10 py-16 lg:grid-cols-[1.15fr_0.85fr]">
          <div>
            <p className="ds-eyebrow">DevStack starter / production-shaped</p>
            <h1 className="mt-5 max-w-4xl text-5xl font-bold leading-none sm:text-7xl">
              {{ project_name }} ships with the boring parts already wired.
            </h1>
            <p className="ds-muted mt-6 max-w-2xl text-lg leading-8">
              FastAPI, Next.js, token auth, protected routes, admin checks, Docker
              Compose, and a clean starter surface that is ready to replace with
              your product workflow.
            </p>
            <div className="mt-10 flex flex-wrap gap-3">
              <Link href="/register" className="ds-pill ds-pill-primary">
                Create account
              </Link>
              <Link href="/dashboard" className="ds-pill ds-pill-outline">
                Open dashboard
              </Link>
            </div>
          </div>

          <div>
            <div className="ds-card p-5">
              <p className="ds-eyebrow">Generated stack</p>
              <div className="mt-6 space-y-4">
                {stackItems.map(([label, value]) => (
                  <div
                    key={label}
                    className="flex items-center justify-between border-b border-[var(--ds-hairline)] pb-4 last:border-0 last:pb-0"
                  >
                    <span className="text-sm opacity-70">{label}</span>
                    <span className="text-sm font-bold">{value}</span>
                  </div>
                ))}
              </div>
            </div>
            <div className="mt-4 grid grid-cols-3 gap-3">
              {["API", "JWT", "RBAC"].map((item) => (
                <div
                  key={item}
                  className="rounded-lg border border-[var(--ds-hairline)] bg-[var(--ds-surface-strong)] px-3 py-2 text-center text-sm font-bold text-[var(--ds-ink)]"
                >
                  {item}
                </div>
              ))}
            </div>
          </div>
        </section>
      </div>
    </main>
  );
}
